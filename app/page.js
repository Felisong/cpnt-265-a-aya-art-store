import Image from "next/image";
import ImageCarousel from "./components/hero-comps/ImageCarousel";
import ProductSection from "./components/products-section/ProductsSection";
import { Button } from "@mui/material";

export default function Home() {
  return (
    <>
      <ImageCarousel />
    </>
  );
}

// hi future me.
// Good plan of execution tomorrow
// 1. ask how to custom colors/ fonts in material ui.
// 2. finish footer
// 3.import forms into formed webpages
// 4. vague layout prep for products
// 5. think about database schemas and see if you can take a look. its ok if not.
